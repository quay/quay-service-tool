import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Login: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      This is the public-facing component.
    </Title>
  </PageSection>
);

export { Login };
